"use client"

import PinboardPortfolioHero from "@/components/ui/pinboard-portfolio-hero"

/** The pinned slip takes any name — the last word is the one inked red. */
export default function DemoNamed() {
  return <PinboardPortfolioHero name="Kedhareswer Naidu" />
}
